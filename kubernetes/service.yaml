apiVersion: v1
kind: Service
metadata:
  name: ai-api
  namespace: mlops-home-assignment
  labels:
    app: ai-api
spec:
  ports:
  - port: 80
    targetPort: 8083
    protocol: TCP
    name: http
  selector:
    app: ai-api